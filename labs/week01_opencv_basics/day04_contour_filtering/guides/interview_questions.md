# Interview Questions

각 질문 아래의 `답변 구조`만 참고하고, 실제 답변은 자신의 실험 결과를 넣어 직접 작성합니다.

## 1. Contour란 무엇인가?

답변 구조:

```text
정의
→ 어떤 입력에서 사용하는지
→ Day 4에서 어떤 역할을 했는지
```

내 답변:

```text
TODO
```

## 2. 왜 contour 검출 전에 이진 마스크를 준비했는가?

답변 구조:

```text
관심 영역과 배경 분리
→ 흰색/검은색 의미
→ 잘못된 마스크가 contour 결과에 미치는 영향
```

내 답변:

```text
TODO
```

## 3. 최소 면적 필터를 높이면 어떤 변화가 생기는가?

답변 구조:

```text
작은 노이즈 감소
→ 오탐 감소 가능성
→ 작은 실제 후보 미탐 가능성
→ 내가 선택한 기준과 근거
```

내 답변:

```text
TODO
```

## 4. 면적만으로 후보를 판단하면 왜 부족한가?

답변 구조:

```text
같은 면적이지만 모양이 다른 사례
→ 종횡비·채움 비율·위치 결합
→ 실제 실패 사례
```

내 답변:

```text
TODO
```

## 5. 하나의 물체가 여러 contour로 갈라졌다면 무엇을 확인할 것인가?

답변 구조:

```text
threshold 결과
→ closing 필요성
→ 블러·텍스처 영향
→ 과도한 전처리의 부작용
```

내 답변:

```text
TODO
```

## 6. 여러 물체가 하나의 contour로 붙었다면 무엇을 확인할 것인가?

답변 구조:

```text
mask 연결 상태
→ opening 또는 거리 기반 분리 가능성
→ 실제 물체 간격
→ 규칙 기반 방식의 한계
```

내 답변:

```text
TODO
```

## 7. 이미지 경계에 닿은 후보를 무조건 제거해도 되는가?

답변 구조:

```text
배경 경계 오탐 제거 장점
→ 실제 물체가 화면 밖으로 걸친 경우 미탐 위험
→ 제거 또는 별도 분류 정책
```

내 답변:

```text
TODO
```

## 8. 일반 바운딩 박스가 회전 물체에서 부정확할 수 있는 이유는?

답변 구조:

```text
이미지 축에 고정된 사각형
→ 빈 공간 증가
→ 종횡비 왜곡
→ 회전 사각형 또는 방향 특징 확장
```

내 답변:

```text
TODO
```

## 9. Contour와 Connected Components의 차이는?

답변 구조:

```text
경계 기반 분석
vs
연결 영역 라벨링
→ 어떤 상황에서 각각 편리한지
```

내 답변:

```text
TODO
```

## 10. 처리 속도는 어떻게 측정했는가?

답변 구조:

```text
측정 구간 정의
→ 순수 처리와 저장 시간 구분
→ 반복 측정 여부
→ 실시간 확장 시 병목 예상
```

내 답변:

```text
TODO
```

## 11. 후보 탈락 이유를 기록한 이유는?

답변 구조:

```text
디버깅 가능성
→ 파라미터 조정 근거
→ 현장 설명 가능성
→ 오탐·미탐 분석
```

내 답변:

```text
TODO
```

## 12. 이 규칙 기반 방식의 한계는 무엇인가?

답변 구조:

```text
조명·해상도·물체 변화에 민감
→ 고정 임계값의 한계
→ 보정, 정규화, 학습 기반 방식으로 확장
```

내 답변:

```text
TODO
```
